#!/usr/bin/bash

git config --global user.name "Kurt Brunner"
git config --global user.email "Kur@Brunner.codes"
git config --global user.signingkey 584C761858F675A7

git config --global commit.gpgsign true
git config --global tag.gpgSign true
